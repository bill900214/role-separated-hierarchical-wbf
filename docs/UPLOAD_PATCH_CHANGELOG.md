# Upload Patch Changelog

This prepared version:

- removes `deim832_aux_daynight.json`, `deim960_aux_daynight.json`, and `df1536_aux_daynight.json`;
- corrects the author line and repository URL;
- fixes YOLOv10 and YOLOR inference defaults/output naming;
- retains the corrected YOLOv13 multi-scale script;
- adds YOLO multi-scale commands;
- adds D-FINE three-stage command wrappers;
- documents the limited DEIMv2-S-960 release scope;
- removes private server account paths from public-facing text/config files;
- replaces checkpoint paths with filenames and checksums;
- identifies verified upstream repositories and licenses without inventing missing commit hashes.
