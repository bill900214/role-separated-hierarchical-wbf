# D-FINE Inference

`infer_dfine_1536.py` is a reference wrapper for generating the retained D-FINE-L-1536 prediction format from an upstream D-FINE checkout.

The exact original shell-history command was not preserved. The retained prediction JSON remains the authoritative experiment artifact:

```text
predictions/transformer_inputs/dfine1536_last_conf005_iou070_official.json
```
