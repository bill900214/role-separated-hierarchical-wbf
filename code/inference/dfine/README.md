# D-FINE Inference

`infer_dfine_1536.py` is an experiment-specific wrapper for D-FINE-L-1536 and
requires an upstream D-FINE checkout and checkpoint.

The corresponding prediction artifact is
`predictions/transformer_inputs/dfine1536_last_conf005_iou070_official.json`.
