# Official Evaluation Records

`official_metrics.csv` records metrics returned by the official AI City Challenge evaluation platform.

The hidden FishEye1K_eval reference annotations are not included, so the official F1 and AP metrics cannot be recalculated locally.

`artifact_checksums.sha256` records SHA-256 values for the retained prediction artifacts.
